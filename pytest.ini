[pytest]
markers =
    aws: marks tests that require AWS credentials
    gcp: marks tests that require GCP credentials
    windows: marks tests that are Windows-specific
    linux: marks tests that are Linux-specific
    macos: marks tests that are macOS-specific
    config: marks tests that require config functionality
    e2e: marks end-to-end tests
